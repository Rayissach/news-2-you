<h1>Helloooo</h1>

{{!-- {{#each saved}}
<button class="btn add-note" data-id="{{_id}}">Add Note</button>
<button class="btn delete-article" data-id="{{_id}}"> Delete Article</button>

{{else}}

<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalCenter" id="save" data-id="{{_id}}">Save Article</button>

{{/each}}

{{!-- <!-- Button trigger modal -->
<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalCenter">
  Launch demo modal
</button> --}}

<!-- Modal -->
<div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLongTitle">Modal title</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
          <div class="container-fluid text-center">
        <form action="/submit" method="post">
            <h4>Notes for article: {{_id}}</h4>
            <ul class="list-group note-container">
                <li class="list-group-item"></li>
            </ul>
            <textarea type="text" placeholder="New Note" name="body" rows="4" cols="60">Write Note Here</textarea>
            </form>
      </div>
      <div class="modal-footer">
        {{!-- <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button> --}}
        <button class="btn delete-article btn-danger" data-id="{{_id}}"> Delete Article</button>
        {{!-- <button type="button" class="btn btn-primary">Save Note</button> --}}
        <button class="btn add-note btn-success" data-id="{{_id}}">Save Note</button>
        <button type="submit" class="btn btn-success save">Save Note</button>
        </div>
      </div>
    </div>
  </div>
</div> --}}